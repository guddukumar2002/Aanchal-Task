{
  "outputDirectory": "dist",
  "installCommand": "npm install && node node_modules/esbuild/install.js",
  "buildCommand": "npm run build",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
